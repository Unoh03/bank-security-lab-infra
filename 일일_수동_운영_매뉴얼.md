# BANK 보안 실험환경 일일 수동 운영 매뉴얼

> 목적: Codex를 사용할 수 없을 때도 운영자가 직접 AWS 환경을 만들고,
> 상태를 확인하고, 실험 증거를 수집한 뒤, 과금 Runtime을 안전하게 삭제한다.
>
> 상세 설계와 예외 절차는
> [`DAILY-CICD-RUNBOOK.md`](./DAILY-CICD-RUNBOOK.md)를 기준으로 한다.

## 1. 가장 먼저 기억할 것

일상 운영에서는 원시 `terraform apply`와 `terraform destroy`를 직접 실행하지
않는다. 아래 Wrapper가 Account 확인, Plan 요약, Foundation 보호, Evidence
수집, 잔존 Resource 검사를 함께 수행한다.

```powershell
# 아침 Preview
.\daily-up.ps1 -RuntimeProfile minimal

# 확인 후 실제 생성
.\daily-up.ps1 -RuntimeProfile minimal -ConfirmApply 'APPLY DAILY'

# Runtime을 건드리지 않고 증거만 수집
.\daily-down.ps1 -EvidenceOnly `
  -ExperimentId '실험이름' `
  -ScenarioId '시나리오이름'

# 하원 전 Preview
.\daily-down.ps1

# 확인 후 실제 삭제
.\daily-down.ps1 -ConfirmDestroy 'DESTROY DAILY'
```

Foundation은 매일 생성하거나 삭제하지 않는다. AWS Account를 이전하거나
Foundation Source를 바꿨을 때만 `setup-foundation.ps1`을 사용한다.

## 2. 현재 기본 환경

| 항목 | 현재 기본값 |
|---|---|
| Terraform Source | `D:\terraform\aws_terraform_build_code` |
| Application Source | `D:\DVWA` |
| AWS Profile | `terra-user` |
| 예상 AWS Account | `433048100798` |
| Primary Region | `ap-northeast-2` |
| DR Region | `ap-northeast-1` |
| Bastion SSH Alias | `bas` |
| Local Evidence | `$HOME\Documents\aws-topology-evidence` |
| Argo CD Local URL | `https://localhost:8080` |

다른 AWS Account에서 실행할 때는 기본값을 그대로 사용하지 않는다. Profile,
Account ID, Seoul·Tokyo EC2 Key Pair 이름과 Bastion Private Key 경로를 명시한다.

## 3. 아침 — Daily Runtime 생성

### 3.1 PowerShell과 Source 확인

일반 PowerShell을 열고 다음을 실행한다.

```powershell
cd D:\terraform\aws_terraform_build_code

git status --short
git pull --ff-only
```

- `git status --short`가 비어 있으면 정상이다.
- 수정 파일이 표시되면 바로 Pull하지 말고 `git diff`로 먼저 확인한다.
- State·Plan·실제 `terraform.tfvars`는 Git에서 제외되므로 표시되지 않는다.

### 3.2 AWS Account 확인

```powershell
aws sts get-caller-identity --profile terra-user
aws configure get region --profile terra-user
```

현재 개인 환경에서는 다음이 맞아야 한다.

```text
Account: 433048100798
Region:  ap-northeast-2
```

Account가 다르면 **중단한다.** 잘못된 Account에서 Plan·Apply·Destroy를 실행하지
않는다.

### 3.3 Terraform State 기준선 확인

```powershell
# Down 상태라면 출력이 없어야 한다.
terraform state list

# Foundation은 남아 있어야 한다.
terraform -chdir=foundation state list
```

현재 Checkpoint에서 Daily State는 `0`, Foundation State는 `27`개였다. Foundation
개수는 Source 확장에 따라 바뀔 수 있으므로 `27` 자체보다 **Daily는 비어 있고
Foundation은 존재한다**는 경계를 확인한다.

Runtime Profile 도입은 DR·Valkey·EFS Resource 주소를 조건부 `count` 형태로
바꾸므로 최초 적용 전 Daily State가 반드시 비어 있어야 한다. 기존 Runtime이
남아 있거나 다른 Account의 State를 가져왔다면 Source만 교체하지 말고 먼저
State migration 필요 여부를 검토한다.

### 3.4 변경 없는 Preview

```powershell
.\daily-up.ps1 -RuntimeProfile minimal
```

이 단계는 Plan만 만들고 AWS Resource를 생성하지 않는다. 다음을 확인한다.

- AWS Account와 Region이 예상값과 일치하는가
- Foundation ECR·OIDC·IAM·로그 보존 계층이 존재하는가
- Plan에 예상하지 않은 `delete` 또는 `replace`가 없는가
- `minimal` 기준 Primary EKS Node 1대·Single-AZ RDS이며 DR·Valkey·EFS가 없는가
- EKS·RDS·VPC·NAT·ALB·CloudFront 등 Daily Runtime 생성이 맞는가
- Foundation Resource가 Daily Plan에 포함되지 않았는가

이상한 삭제·교체가 하나라도 있으면 실제 Apply를 실행하지 않는다.

### 3.5 실제 생성

```powershell
.\daily-up.ps1 -RuntimeProfile minimal -ConfirmApply 'APPLY DAILY'
```

관측된 Cold Up 시간은 약 34분이었다. AWS 상황에 따라 더 오래 걸릴 수 있다.
진행 중에는 다음을 지킨다.

- 같은 명령을 두 번째 창에서 다시 실행하지 않는다.
- `terraform.exe`를 강제 종료하지 않는다.
- State Lock을 임의로 삭제하거나 `force-unlock`하지 않는다.
- 노트북 전원과 Network를 유지한다.

별도 PowerShell에서 Process만 확인할 수 있다.

```powershell
Get-Process terraform -ErrorAction SilentlyContinue
```

활성 Daily Session 상태는 다음에 저장된다.

```text
%LOCALAPPDATA%\aws-topology\daily-session\active-session.json
%LOCALAPPDATA%\aws-topology\daily-session\logs\<session-id>.log
%LOCALAPPDATA%\aws-topology\daily-session\logs\<session-id>.daily-down.<attempt-id>.log
```

마지막 파일은 Watchdog이 실행한 Down 시도별 Sanitized stdout/stderr, 남은
Terraform State 주소와 Project Tag 기반 AWS Runtime을 보존한다. Retry Window가
끝나면 자동 재시도를 멈추므로 이 파일과 실제 AWS Runtime을 먼저 대조한다.

`daily-up.ps1`은 기본 `-WatchdogMode On`에서 독립 Watchdog을 등록한다. 기본
시간 제한은 다음과 같다.

- 5시간: 새 변경·공격을 시작하지 않고 정리 준비
- 6시간: Evidence와 Daily Down 우선
- Terraform Process·Lock 존재 시 15분 간격, 최대 2시간만 제한 재시도

자동 Down이 필요 없는 평일에는 다음처럼 끌 수 있다.

```powershell
.\daily-up.ps1 -RuntimeProfile minimal -WatchdogMode Off `
  -ConfirmApply 'APPLY DAILY'
```

`Off`는 안전 확인과 Deadline 기록을 끄지 않는다. 자동 Scheduled Task만 만들지
않으므로 하원 전 수동 Daily Down 책임은 운영자에게 남는다. `minimal` 외에 DR
실험은 `dr-test`, 기존 고가용성 재현은 `full`을 사용하며 실행 중 Profile은 바꾸지
않는다. Valkey·EFS는 필요가 증명된 경우에만 `-EnableValkey`, `-EnableEfs`로
활성화한다. HTTP 허용 실험은 `-AllowHttp`를 명시하며 평상시에는 사용하지 않는다.

## 4. 생성 완료 확인

### 4.1 Script 성공 문구

정상 종료 시 최소한 다음을 출력한다.

```text
Daily up completed.
Argo CD: Synced / Healthy
Application 'dvwa': Ready
URL: https://...
```

중간 단계가 실패했는데 이 문구가 없다면 Cold Up 성공으로 기록하지 않는다.

### 4.2 Terraform Output

```powershell
terraform output -raw application_url
terraform output -raw seoul_bastion_public_ip
terraform output -raw primary_eks_cluster_name
terraform output -raw dr_eks_cluster_name
```

`primary_db_bootstrap`은 Sensitive Output이다. 화면에 출력하거나 채팅·Vault·Git에
붙여 넣지 않는다.

### 4.3 Bastion과 Kubernetes

```powershell
ssh bas
```

Bastion에서:

```bash
kubectl get nodes -o wide
kubectl get pods -A
kubectl -n dvwa get deployment,pod,service
kubectl -n argocd get application dvwa
```

정상 기준:

- Primary Node가 `Ready`
- Argo CD Pod가 `Running`
- Primary Node의 Fluent Bit Pod가 `Running`
- BANK DVWA Pod가 `Running`, `READY 1/1`
- Argo Application이 `Synced`, `Healthy`

DR Cluster 상태는 Primary Bastion의 현재 kubeconfig 설정에 따라 별도 Context가
필요할 수 있다. Context를 확인하지 않고 Cluster를 단정하지 않는다.

### 4.4 Argo CD Web UI

새 로컬 PowerShell에서:

```powershell
cd D:\terraform\aws_terraform_build_code
.\argocd-ui.ps1
```

- Username: `admin`
- Password: Clipboard에 자동 복사되며 화면에는 출력되지 않는다.
- Browser에서 `https://localhost:8080`을 연다.
- 이 PowerShell 창을 닫거나 `Ctrl+C`를 누르면 SSH Tunnel도 종료된다.

UI에서 확인할 것:

- Application: `dvwa`
- Sync: `Synced`
- Health: `Healthy`
- Deployment → ReplicaSet → Pod 계보
- 실행 중 Image가 Git에 선언된 immutable `sha-<commit>`인지 여부

## 5. 보안 실험과 증거 수집

### 5.1 실험 전에 기록할 것

```powershell
$startUtc = (Get-Date).ToUniversalTime()
$startUtc
```

함께 기록한다.

- Experiment ID
- Scenario ID
- 정상 Baseline
- 공격·이상행위
- 예상 차단·통과 지점
- 조치 전·후에 고정할 입력 조건

Password·Cookie·Session ID·Authorization Header·Access Key·DB Credential은
기록하지 않는다.

### 5.2 가장 간단한 수집 — 최근 60분

```powershell
$experimentId = 'team-sqli-before-01'

.\daily-down.ps1 `
  -EvidenceOnly `
  -ExperimentId $experimentId `
  -ScenarioId 'TEAM-SQLI-01'
```

`-EvidenceOnly`는 Terraform을 변경하거나 Runtime을 삭제하지 않는다.

### 5.3 정확한 실험 시간만 수집

실험 종료 직후:

```powershell
$endUtc = (Get-Date).ToUniversalTime()

.\daily-down.ps1 `
  -EvidenceOnly `
  -ExperimentId 'team-sqli-before-01' `
  -ScenarioId 'TEAM-SQLI-01' `
  -EvidenceStartUtc $startUtc `
  -EvidenceEndUtc $endUtc
```

저장 위치:

```text
C:\Users\Unoh\Documents\aws-topology-evidence\team-sqli-before-01\
```

최소 확인 파일:

```text
manifest.json
SHA256SUMS.txt
source\
queries\
results\
sanitized\
```

일부 Source가 비어 있으면 공격이 없었다고 바로 단정하지 않는다. 해당 Source가
수집 대상이었는지, 전달 지연이 있는지, Application Audit가 구현된 Route인지
구분한다.

### 5.4 Query Pack 사용 경계

현재 `-RunEvidenceQueries`가 바로 연결된 관측 후보는 `WEB-01`, `IAM-01`이다.

```powershell
.\daily-down.ps1 `
  -EvidenceOnly `
  -RunEvidenceQueries `
  -ExperimentId 'observability-web-check' `
  -ScenarioId 'WEB-01'
```

새 팀 시나리오는 `automation\project.psd1`에 Query Mapping이 없으면
`-RunEvidenceQueries`를 붙이지 않는다. 먼저 기존 Query를 재사용할지, 새 Query가
필요한지 결정한다.

Athena Query는 S3 Scan 비용과 Glue Catalog 변경이 있으므로 단순 Evidence 수집과
구분한다. 임의 SQL이 아니라 검토된 네 Query만 실행한다.

## 6. 하원 전 — Daily Runtime 삭제

### 6.1 먼저 Preview

```powershell
cd D:\terraform\aws_terraform_build_code
.\daily-down.ps1
```

확인할 것:

- 현재 AWS Account가 예상값인가
- 삭제 대상이 Daily Runtime인가
- Foundation Resource가 포함되지 않았는가
- 아직 수집하지 않은 실험 Evidence가 있는가
- Terraform Process·State Lock이 없는가

### 6.2 실제 삭제

```powershell
.\daily-down.ps1 -ConfirmDestroy 'DESTROY DAILY'
```

Wrapper는 다음을 순서대로 수행한다.

```text
Pre-Destroy Evidence
→ Fresh Destroy Plan
→ Foundation 보호 검사
→ Karpenter NodePool·NodeClaim 삭제와 EC2 종료 대기
→ Daily Terraform Destroy
→ State·AWS 잔존 Runtime 검사
→ Post-Destroy Evidence
→ Watchdog·활성 Session 제거
```

### 6.3 종료 확인

정상 출력:

```text
Daily down completed.
Daily Terraform state: empty
Tracked daily AWS residue: none
Tagged daily AWS runtime: none
```

추가 확인:

```powershell
# 0이어야 한다.
(terraform state list | Measure-Object).Count

# Foundation은 남아 있어야 한다.
(terraform -chdir=foundation state list | Measure-Object).Count

# 출력이 없어야 한다.
Get-Process terraform -ErrorAction SilentlyContinue

# Source 변경이 없다면 비어 있어야 한다.
git status --short
```

Down이 실패했거나 AWS 잔존 Resource가 출력되면 성공으로 기록하지 않는다.

## 7. 자주 발생하는 문제

### AWS Account가 다름

**행동:** 즉시 중단한다. Profile을 추측해서 바꾸거나 Apply하지 않는다.

```powershell
aws configure list-profiles
aws sts get-caller-identity --profile PROFILE_NAME
```

### `git status --short`에 파일이 나옴

**행동:** Pull 전에 변경 내용을 확인한다.

```powershell
git diff
git diff --stat
```

State·Plan·Credential을 `git add -f`로 강제로 등록하지 않는다.

### Apply가 오래 걸림

EKS와 RDS는 생성에 오래 걸릴 수 있다. 동일 Apply를 또 실행하지 않는다.

```powershell
Get-Process terraform -ErrorAction SilentlyContinue
```

Process가 실행 중이면 기다린다. 화면이 조용하다는 이유만으로 종료하지 않는다.

### Apply가 중간 실패함

1. 오류 원문과 발생 시각을 로컬에 보존한다.
2. Terraform Process가 끝났는지 확인한다.
3. 현재 State를 확인한다.
4. 비용 Runtime을 제거해야 한다면 `daily-down.ps1` Preview부터 실행한다.

```powershell
Get-Process terraform -ErrorAction SilentlyContinue
terraform state list
.\daily-down.ps1
```

바로 `terraform destroy`, `force-unlock`, State 삭제를 실행하지 않는다.

### Down이 Karpenter EC2 때문에 실패함

`daily-down.ps1`은 확인 문구 뒤 Terraform Destroy 전에 Karpenter NodePool과
NodeClaim을 제거하고 해당 EC2가 0대가 될 때까지 제한 시간만 기다린다. 이미
Cluster가 사라졌다면 `Project`, Cluster ownership, `karpenter.sh/*`,
`ManagedBy=Karpenter` Tag가 모두 일치하는 고아 Node만 정리 범위로 인정한다.

Tag가 불충분하면 다른 EC2를 잘못 종료하지 않도록 실패한다. 이 경우 임의 종료나
State 삭제를 하지 말고, 위의 `daily-down.<attempt-id>.log`와 AWS EC2 Tag를 먼저
대조한다.

### `ssh bas` Timeout

```powershell
terraform output -raw seoul_bastion_public_ip
```

`daily-up.ps1`은 성공 과정에서 SSH Config의 정확한 `Host bas` Block을 현재 IP로
갱신한다. Up이 중간 실패했다면 Alias가 갱신되지 않았을 수 있다.

### Argo CD가 `Synced / Healthy`가 아님

Bastion에서:

```bash
kubectl -n argocd get applications
kubectl -n dvwa get deployment,pod,service
kubectl -n dvwa get events --sort-by=.lastTimestamp | tail -n 30
```

`ImagePullBackOff`라면 Pod 이름을 확인하고:

```bash
kubectl -n dvwa describe pod POD_NAME
```

ECR Image 존재, Image URI·Tag, Node의 ECR Pull 권한을 분리해 확인한다.

### Evidence Source가 0건임

0건은 곧 실패가 아니다.

- 정확한 UTC Window였는가
- AWS 로그 전달이 아직 지연 중인가
- WAF Filter가 해당 Action을 저장하는가
- 해당 Route에 BANK Audit Event가 구현됐는가
- 그 행위가 DB 내부·Browser·Container Process처럼 현재 Blind Spot인가

확인되지 않은 Source를 추측으로 채우거나 시나리오 성공으로 기록하지 않는다.

### Daily Down 실패

- 실패를 성공으로 보고하지 않는다.
- Foundation이 포함된 Plan이면 실행하지 않는다.
- Terraform Process·Lock이 있으면 강제 종료·강제 해제하지 않는다.
- 남은 Resource ID와 과금 가능성을 기록한다.
- 노트북을 끄기 전에 AWS Console 또는 개별 AWS API로 잔존 상태를 확인한다.

## 8. 절대 하지 않을 것

일상 운영 중 다음 명령은 사용하지 않는다.

```text
terraform apply
terraform destroy
terraform force-unlock
terraform state rm
terraform import
terraform apply -target=...
terraform destroy -target=...
```

State 이전·복구처럼 예외적인 작업은 State Backup, 대상 주소, Source와 실제 AWS
객체를 모두 확인한 별도 절차로 수행한다.

또한 다음 파일이나 값을 Git·Vault·채팅·Evidence에 넣지 않는다.

```text
.terraform/
terraform.tfstate*
*.tfplan
실제 *.tfvars
Private Key
kubeconfig
AWS Credential
DB Password
Cookie·Session ID·Authorization Header
```

## 9. 한 페이지 요약

```powershell
cd D:\terraform\aws_terraform_build_code

# 1. Source·Account
git status --short
git pull --ff-only
aws sts get-caller-identity --profile terra-user

# 2. Preview·Up
.\daily-up.ps1 -RuntimeProfile minimal
.\daily-up.ps1 -RuntimeProfile minimal -ConfirmApply 'APPLY DAILY'

# 3. 상태 확인
ssh bas
# Bastion: kubectl get nodes && kubectl get pods -A

# 4. 필요할 때 Evidence
.\daily-down.ps1 -EvidenceOnly `
  -ExperimentId '실험이름' `
  -ScenarioId '시나리오이름'

# 5. Preview·Down
.\daily-down.ps1
.\daily-down.ps1 -ConfirmDestroy 'DESTROY DAILY'

# 6. 종료 확인
(terraform state list | Measure-Object).Count
(terraform -chdir=foundation state list | Measure-Object).Count
Get-Process terraform -ErrorAction SilentlyContinue
```

최종 원칙:

> Account가 다르거나, Plan이 예상과 다르거나, Terraform Process·Lock이 있거나,
> Foundation이 삭제 대상에 포함되면 실행하지 않는다.
